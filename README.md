# DesktopStream_SS

This repo follows Clean Architecture system design. It has models folder which contains all the `core` models of an application, routes and services are of the application `layer` and infrastructure is `infrastructure`.

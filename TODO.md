# Basic operations are

1. Clients connect to the server, server must store them in whatever storage it can acquire
2. A client send a ping to a client to check if both are ready for a P2P connection or not
3. A client create an offer for a client, this essentially helps client send its SDP offer
4. A client accepts/rejects an SDP offer of another client
